[project]
name = "bq-schema-generator"
version = "0.1.0"
description = "Generate BigQuery schemas from Firestore collections"
requires-python = ">=3.8"
dependencies = [
    "google-cloud-firestore>=2.11.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
