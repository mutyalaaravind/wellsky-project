# Extract and Fill

Extract and Fill Widget & microservice

## Running via docker-compose

1. gcloud auth login
2. gcloud auth application-default login
3. cd extract-and-fill && make run

## Running widget without docker

1. nvm use 18
2. cd widget && npm i && npm start

## Running api without docker

1. make run-no-docker
